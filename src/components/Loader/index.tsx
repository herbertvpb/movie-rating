import React from 'react';
import { LoaderContainer, Spinner } from './styles';

const Loader = () => {
  return (
    <LoaderContainer>
      <Spinner />
    </LoaderContainer>
  );
};

export default Loader;
